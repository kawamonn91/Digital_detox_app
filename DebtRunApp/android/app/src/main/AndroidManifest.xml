<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 一日のまとめ通知(Android 13以降は実行時に許可を求める) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 再起動後に一日のまとめ通知を予約し直す -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- notifee が追加する「正確なアラーム」の権限は使わない(まとめ通知は数分のずれを許す通常のアラームで予約する) -->
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        tools:node="remove" />

    <!-- スクリーンタイム取得(「使用状況へのアクセス」画面でユーザーが許可する) -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- GPS ランニング計測。計測中は前面サービス(通知)を出して、画面を消しても計測を続ける -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- スクリーンタイムからホーム画面(ランチャー)にいた時間を除くため、ホームアプリを調べる -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.HOME" />
        </intent>
    </queries>

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="${usesCleartextTraffic}">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- まとめ通知のタップで debtrun://summary を開く -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="debtrun" android:host="summary" />
            </intent-filter>
        </activity>

        <!-- スクロール計測(アクセシビリティサービス) -->
        <service
            android:name=".ScrollTrackerService"
            android:label="DebtRun スクロール計測"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- 一日のまとめ通知(予約した時刻の通知・再起動後の予約し直し) -->
        <receiver
            android:name=".DailySummaryReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- ランニング計測中の前面サービス。notifee の既定(shortService: 最長3分)を位置情報用に置き換える -->
        <service
            android:name="app.notifee.core.ForegroundService"
            android:exported="false"
            android:foregroundServiceType="location"
            tools:replace="android:foregroundServiceType" />

    </application>
</manifest>
